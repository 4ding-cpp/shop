/* ============
 * Mutations for the auth module
 * ============
 *
 * The mutations that are available on the
 * account module.
 */



export default {
  /**
   * set config
   * @param {*} state 
   * @param {*} o 
   */
  set_config(state, o) {
    state.config = o
  },
}
